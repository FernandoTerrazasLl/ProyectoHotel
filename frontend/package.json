{
  "name": "hotel-booking-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite src --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:coverage:empty": "vitest run --coverage --passWithNoTests nonexistent"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-istanbul": "^3.0.5",
    "eslint": "^10.4.0",
    "jsdom": "^26.0.0",
    "vite": "^6.1.1",
    "vitest": "^3.0.5"
  }
}
